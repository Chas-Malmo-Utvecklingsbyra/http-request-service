# HTTP-Request-Service
## Chas Malmö Utvecklingsbyrå

## Summary
* `HTTP Request Service handles:`
* `- Fetching weather data from Open-Meteo`
* `- Continues to fetch every 15 minutes`
* `- Displays in the terminal when the next update is due`   

## Usage
* `Is automatically included into the Energy-Saver-Deluxe repo when running "premake5 install"`
